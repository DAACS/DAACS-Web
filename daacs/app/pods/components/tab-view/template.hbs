{{yield (hash
    list=(component "tab-list" tabView=this)
    item=(component "tab-item" tabView=this)
    panels=(component "tab-panels" tabView=this)
    panel=(component "tab-panel" tabView=this)
)}}
