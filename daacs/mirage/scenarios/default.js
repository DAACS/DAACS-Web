export default function(/*server*/) {

    /*
    Seed your development database using your factories.
    This data will not be loaded in your tests.

    Make sure to define a factory for each model you want to create.
    */
    //server.create('oauth-token');
    //server.createList('assessment-summary', 4);
    //server.createList('user-assessment-summary', 1);
    //server.createList('user-assessment-question-group', 4);
    //server.createList('user-assessment-writing-sample', 1);
    //server.createList('user', 5);
    //server.createList('assessment-content', 4);
}
