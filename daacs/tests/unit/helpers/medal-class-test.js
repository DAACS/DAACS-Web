import { medalClass } from 'daacs/helpers/medal-class';
import { module, test } from 'qunit';

module('Unit | Helper | medal class');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = medalClass(['HIGH']);
  assert.ok(result);
});
