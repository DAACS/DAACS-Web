import { renderHtml } from 'daacs/helpers/render-html';
import { module, test } from 'qunit';

module('Unit | Helper | render html');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = renderHtml([42]);
  assert.ok(result);
});
